package com.oscleton.sdk.enums

/**
 * Enum class representing a send state, either ENABLED, DISABLED, IRRELEVANT
 *
 * @since 0.8
 */
enum class SendState {
    ENABLED,
    DISABLED,
    IRRELEVANT
}