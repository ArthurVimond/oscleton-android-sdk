package com.oscleton.sdk.enums

/**
 * Constants class representing the application track
 * used for the Oscleton MIDI Remote Script auto-update process.
 *
 * @since 0.8
 */
object AppTrack {
    const val PRODUCTION = "production"
    const val BETA = "beta"
    const val ALPHA = "alpha"
    const val INTERNAL = "internal"
    const val DEV = "dev"
}